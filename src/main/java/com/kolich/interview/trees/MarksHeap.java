package com.kolich.interview.trees;

/**
 * Each child of a node has a value less than or equal to that
 * of itself.  Consequently, the root node always has the largest
 * value, so you can find the largest value in constant time.
 */
public class MarksHeap<T> {

}